#include "main.h"
void _puts(char *str);
#include<stdio.h>
/**
* main - Entry point
*_put: void
* Return: Always 0 (Success)
*/
int main(void)
{
	_puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
