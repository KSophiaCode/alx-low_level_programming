#include "main.h"
void _puts(char *str);
#include<stdio.h>
/**
* main - Entry point
*_put: write the character c to stdout
* Return: Always 0 (Success)
*/
int main(void)
{
	_puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
